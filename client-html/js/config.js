// Basic Configuration
const CONFIG = {
    // If running locally, this is likely http://localhost:5000
    // If deployed, this should be the deployed backend URL
    API_URL: 'http://localhost:5000' 
};
